{
  "mcpServers": {
    "manus-mcp": {
      "command": "bash",
      "args": ["-c", "cd ${workspaceFolder} && source .venv/bin/activate && python run.py"],
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  }
} 